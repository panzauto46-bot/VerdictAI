const puppeteer = require('puppeteer'); (async () = const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173/dashboard'); await page.waitForSelector('body'); console.log(await page.evaluate(() =, 500))); await browser.close(); })();  
